services:
    api: &api
        build:
            context: .
            dockerfile: ./docker/django/Dockerfile
        image: ghcr.io/mitch1802/${DOCKER_REPO}:api-${VERSION}

    postgres:
        build:
            context: .
            dockerfile: ./docker/postgres/Dockerfile
        image: ghcr.io/mitch1802/${DOCKER_REPO}:db-${VERSION}

    nginx:
        build:
            context: .
            dockerfile: ./docker/nginx/Dockerfile
        image: ghcr.io/mitch1802/${DOCKER_REPO}:nginx-${VERSION}
